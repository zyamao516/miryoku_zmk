/* config/boards/arm/nice_nano/nice_nano.overlay */

/* Provide the label that the nice_view shield expects */
nice_view_spi: &spi1 {
    status = "okay";
    /* Default assumption (matches the adapter README): CS on Pro Micro D1 */
    cs-gpios = <&pro_micro 1 GPIO_ACTIVE_LOW>;
};
